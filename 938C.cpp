#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t,i,j,x,y,z,m,n,o,p,ans;
    cin>>t;
    while(t--)
    {
        int N;
        long long k;
        cin>>N>>k;
        std::vector<long long> a(N)
        for(i=0;i<N;i++)
            cin>>a[i];
        while(k!=0)
        {

        }
    }
    return 0;
}s
